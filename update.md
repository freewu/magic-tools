# MagicTools v2.12.0

## 更新内容

### ✨ 新增

- 「正则表达式」新增「**代码生成**」页签：底部说明区改为「说明 / 代码生成」双页签，选好语言即按当前正则 + `i/g/m/s` 标志位（连同上方文本）生成对应语言的调用代码，支持 **21 种语言**（Python / C / C++ / Java / C# / JavaScript / R / Rust / Delphi / PHP / Go / Ruby / Swift / Perl / Objective-C / Julia / Kotlin / TypeScript / Erlang / Lua / PowerShell），带 **highlight.js 语法高亮**与一键复制。每个语言都按自身惯例生成（原始字符串、内联标志位、选项枚举、全局匹配方式差异；模式里出现引号或 `/` 等分隔符时自动降级转义），示例文本取上方输入内容、最多前 6 行
- 新增「**提词器**」工具（其它）：调速 / 字号 / 行距 / 淡入淡出 / 全屏，空格开始·暂停；默认示例稿按语言随机载入示范诗；参数可「保存为默认设置」，也可在 设置 → 其它 → 提词器 里预设默认值
- 提词器新增「**逐行焦点高亮**」（只高亮当前阅读行，越远越透明越淡）与「**按阅读进度从左到右逐字点亮**」
- 新增「**Mermaid 编辑器**」工具（格式化）：实时预览 + 源码编辑，内置 **32 个示例**（覆盖 mermaid 可预览的全部图形，按图形家族分组），支持缩放 / 背景 / 面板开关与导出
- 新增「**CSR 申请文件**」工具（站长工具）：本机生成 RSA 私钥 + PKCS#10 请求，支持 SAN
- 新增「**图片水印**」工具（图片）：文字 / logo 水印，九宫格·平铺 + 旋转·透明度·描边，PNG·JPEG·WebP 导出
- 新增「**图片负片**」「**图片黑白化**」「**图片锐化**」「**图片取色**」四个图片工具：负片可调强度反相；黑白化支持灰度 / 二值与大津法自动阈值；锐化支持 USM 半径 / 强度 / 阈值；取色带放大镜与 HEX/RGB/HSL 及历史色
- 新增「**DNS 查询**」「**Whois 查询**」「**MTR 查询**」三个站长工具（**仅桌面版**）：DNS 支持多记录类型与指定服务器；Whois 走 3 跳 referral 链；MTR 即 traceroute + ping，逐跳给出丢包 / RTT / 抖动
- 「IP 转换」新增 **IPv6 写法**换算
- 新增「**生成器**」分类（二维码 / 条形码 / 密码 / OTP / htpasswd / 数据生成 / 点阵字 / 数独 / 字帖 / Cron），「IP 转换」改归「类型转换」

### 🐞 修复

- 「时间戳转换」上月末 / 下月末时间取成 00:00:00（应为 23:59:59）；顺带修复上月 / 下月日期不存在时年份算错的问题（3-31 的上月应为 2-28、1-31 的下月应为 2-28）
- 「Mermaid 编辑器」缩放与背景显示异常、面板开关失效
- 正则表达式输入框占位符中的 `\d` 未转义（界面上显示成了 `d`）

### 🛠 体验优化

- 「网页 TDK 信息检测」改为**仅桌面版**（应用中心卡片会标注「仅桌面版」）
- 去掉 7 个工具顶部的说明区，说明统一收进工具底部 / 说明页；「数独生成器」打印按钮改名
- 「JSONL 转换」的选择文件 / 载入示例 / 缩进 / 忽略空行移到上方 JSON 框之上的工具条
- 「时间戳转换」GPS / 北斗 / 伽利略 各用分组框展示（内部标签简化为 周+秒 / 周,秒 / 总秒数）

# MagicTools v2.11.0

## 更新内容

### ✨ 新增

- 新增「**JSONL 转换**」工具（类型转换）：JSON 数组 ↔ JSONL 互转。页面为**上方 JSON 框 / 下方 JSONL 框**双框布局，两个框都既可输入也可输出（结果写进目标格式的框，源内容保留）；支持 2 空格 / 4 空格 / Tab 缩进、忽略空行、拖拽或选择文件载入、双击复制、载入示例
- 「数据生成」新增 **.jsonl 输出格式**（每行一条紧凑 JSON，便于导入大数据平台）
- 「图片尺寸调整」更名为「**图片调整**」，并新增 **90° / 180° / 270° 旋转**与 **WebP 输出格式**（WebP 可选输出质量，浏览器不支持时该选项自动禁用）

### 🐞 修复

- 修复所有「保存 / 导出文本」类工具在桌面版**保存对话框选好路径后却没有生成文件**的问题：capabilities 缺少 `fs:allow-write-text-file`（`writeTextFile` 走的是 `write_text_file` 命令），写入被拒后又被静默回退为浏览器下载，看起来「保存了但没有文件」。现在补齐权限，并增加字节写入（`writeFile`）兜底，真失败会明确报错。影响工具：配置转换、数据生成、JSON / JSON5 / SQL 格式化、RSA / SM2 密钥导出、robots.txt、htpasswd、点阵字库、图像取色、WebSocket 调试等
- 修复「Markdown 编辑器」**深色模式下预览区标题（h1 - h6）与正文几乎看不清**的问题

### 🛠 体验优化

- 「JSONL 转换」：**点错按钮或把内容放进另一个框**时会自动按正确方向转换并给出提示，不再只提示「请先输入内容」
- 「JSONL 转换」：「下载」改为弹出**系统保存对话框**（可选目录与文件名，扩展名随结果框为 `.jsonl` / `.json`），Web 版回退浏览器下载
- `just release`：输出**开始时间 / 结束时间 / 耗时**

# MagicTools v2.10.1

## 更新内容

### ✨ 新增

- 字帖生成器：新增「**按行填充**」开关 —— 一行只练一个字，同一行重复同一个字、第 N 行用第 N 个字；配合「循环填充」决定超出字数的行是重复还是留空，适合逐字练习
- 字帖生成器：格线颜色新增「**淡绿**」（四种颜色均为浅色，打印后不干扰书写）
- 设置中心 → 其它 → 字帖生成器：新增「**默认格间距**」「**默认循环填充**」「**默认按行填充**」三项（此前「默认循环填充」只在说明里提到，界面上无法配置）

### 🐞 修复

- 字帖生成器：**字在格子里偏右 / 偏下**（未真正居中）—— 中文字体的字形在字身框里本身不居中（实测楷体「落」偏右约 4.7%、偏下约 1.3%），只靠 flex 居中必然整体偏移；现在会按字逐个光栅化实测墨迹包围盒，再按字逐个反向微调，使米字格十字正好穿过字的中心（实测 93mm 格子 / 67mm 字号下：偏右 2.16mm → 0.27mm、偏下 0.84mm → 0.09mm，残差约 1 个屏幕像素）
- 字帖生成器：**上传的自定义字体不生效** —— 生成 `font-family` 时双引号未转义，导致整个 `style` 属性被截断、字体设置被丢弃；同时自定义字体为异步加载，现在加载完成后会自动重新测量字形偏移
- 侧边栏：折叠状态下鼠标悬停展开的二级菜单浮层超出窗口底部时被裁掉（「其它」分类的子项看不到也点不到），现在会自动向上翻转并留出安全边距

### 🛠 体验优化

- 字帖生成器：设置面板布局重排，文本输入移到最上方；「格间距」支持 0 - 5mm（步进 0.5mm）并带单位提示，自定义字体加载后即时生效
- 字帖生成器：关闭「循环填充」时统计文案直接显示实际填充格子数（如「共 240 格 · 14 字 · 不循环: 14 格有字」），并提示「按行填充」的作用，避免误以为只填了一个字

# MagicTools v2.10.0

## 更新内容

### ✨ 新增

- 数独生成器：新增工具 —— 随机生成**唯一解**的数独题（4×4 / 6×6 / 9×9 三种宫格），难度分高 / 中 / 低；按 A4 版式自动排布（4 宫 4 题 / 6 宫上下 2 题 / 9 宫 1 题），可选「仅题目 / 仅答案（红色）/ 题目 + 答案」，一键「打印 A4」调用系统打印对话框（也可「另存为 PDF」）
- 字帖生成器：新增工具 —— 生成可打印的 A4 练字字帖，支持**米字格 / 田字格 / 回宫格 / 作文格**四种格型与楷体 / 行楷 / 隶书 / 宋体 / 黑体 / 仿宋 / 微软雅黑七种字体，可上传本地 `ttf / otf / woff / woff2` 字体；内容模式含描红 / 黑字 / 首字示范 / 空白格，格线可选红 / 灰 / 蓝
- 打印辅助模块 `src/lib/print.ts`：用离屏 iframe 承载 A4 版式 HTML 再唤起系统打印，与应用页面的深色主题样式完全隔离，打印结果恒为白底（桌面端直连打印机，Web 版走浏览器打印预览）

### 🛠 体验优化

- 数独生成器：出题为「随机回溯生成完整解 → 中心对称挖空 → 校验唯一解」三步，保证每道题只有唯一解；打印答案时题目原有提示数为黑色、填入答案为红色，方便对照批改
- 数独生成器：宫与宫之间的边框线加粗，打印后便于分辨题目边界
- 字帖生成器：格子尺寸由「每行格数 × 每页行数」自动均分 A4 可用区域（约 186mm × 253mm），保证整页不溢出、不裁切；相邻格子共用一条线，避免双线或粗细不均
- 设置中心 → 其它：新增「数独生成器」面板（默认宫格 / 默认难度 / 默认页数 / 默认打印内容）与「字帖生成器」面板（默认格型 / 默认字体 / 默认格线颜色 / 默认内容模式 / 默认每行格数 / 默认每页行数 / 默认页数 / 默认文本）

### 📝 说明

- 工具总数 113（含内置页面共 116 个工具目录），其中「其它」分类 17 个；三个 README 与官网（GitHub Pages）的分类计数、工具清单已同步

# MagicTools v2.9.0

## 更新内容

### ✨ 新增

- 图片分割：新增工具 —— 把一张图片按 2 / 3 / 4 / 6 / 9 份切成整齐的小图，支持 8 种分割方向（1×2 ~ 3×3）、顺序编号 / 行列编号、文件名前缀、每块输出宽度，一键「保存全部到文件夹」（桌面版选择目录一次写入）
- 图片尺寸调整：新增工具 —— 按比例（1-400%，含 25% / 50% / 75% / 100% 快捷按钮）或按像素缩放图片，支持锁定宽高比、不放大图片（仅缩小）、PNG / JPEG 输出与 JPEG 质量调节，缩小超过一半时分多步绘制以避免锯齿

### 🛠 体验优化

- 图片分割：预览按原图布局紧密拼合成整图，每块左上角叠加半透明序号（顺序编号显示 1 / 2 / 3 …，行列编号显示 r1c1）；新增「图块间隔」0-12（默认 2）用于在预览中画出分割线，设为 0 即无缝拼合
- 图片分割：移除单张保存按钮，统一改为「保存全部到文件夹」；修改文件名前缀 / 编号方式不再重新切割画布，预览即时更新
- 图片尺寸调整：设置中心 → 图片 新增「图片尺寸调整」面板，可配置默认缩放比例 / 默认输出格式 / 默认 JPEG 质量 / 默认锁定宽高比

### 🐛 修复

- 多语言：补齐遗漏词条并修复未走 `t()` 的硬编码文案 —— 图片主题色色块提示、Blowfish ~ RC6 偏移量单位、JSON / JSON5 保存标题、RSA / SM2 密钥文件、WebTDK 检测提示、ASCII 文字与 ASCII 图片说明、Markdown 空内容占位等

### 📝 说明

- 工具总数 111（含内置页面共 114 个工具目录），其中「图片」分类 12 个；三个 README 与官网（GitHub Pages）的分类计数、工具清单已同步

# MagicTools v2.8.0

## 更新内容

### ✨ 新增

- 数据生成：新增工具 —— 按 Mock.js 语法生成测试数据，可输出 JSON / CSV / SQL，支持模板、区间、自增、枚举、占位符等规则，设置中心可配置默认格式 / 默认表名 / 默认生成数量
- WebSocket 调试：新增工具 —— 浏览器原生 WebSocket 调试台，支持 ws/wss 连接、心跳保活、JSON 美化、收发日志复制与下载（纯前端，无需后端）
- 图片主题色：颜色值支持 8 种格式展示与导出（HEX / RGB / HSL / CMYK / HSV / LAB / LCH / XYZ），默认 HEX，默认值可在 设置中心 → 图片 → 图片主题色 中修改

### 🛠 体验优化

- 图片主题色：重构为「统计像素 → 合并相近色 → 按占比排序」，可调相似度级别（1-10）、输出颜色数（1-64）、是否忽略透明像素与 α 阈值；结果按占比降序列出颜色色块、色值、占比与像素数，支持一键复制、复制全部、下载 CSV、下载色卡 PNG
- 图片主题色：从「其它」分类移入「图片」分类，并正式登记到工具注册表（此前在应用中心/侧边栏不可见）；移除旧的悬浮球样式，改为色卡列表 + 占比条
- ICO 生成：新增 128×128 尺寸选项
- 代码截图：新增「默认填充」设置（默认 8 px）与「最小宽度」设置（0 = 自适应），导出图片改为系统保存对话框
- App Icon 生成：三平台选项卡片改为 CSS Grid，高度严格一致
- ASCII 文字：新增「默认文字」设置，页面初始内容可在设置中心配置
- 密码生成器：密码默认可见
- 正则测试：新增「清空内容」按钮
- 设置中心：二维码 / 条形码 / 占位图片 等滑块行与 App Icon 选项行对齐修正
- 内置页面（应用中心 / 设置 / 帮助）不再显示面包屑导航

### 📝 说明

- 新增工具需两步登记：`src/App/index.tsx` 的工具注册表 `list` + `src/App/lang-packs.ts`，仅新增目录不会被自动发现（三个 README 已补充说明）
- 工具总数 109（含内置页面共 112 个目录），分类计数已同步修正

# MagicTools v2.7.3

## 更新内容

### ✨ 新增

- 颜色格式转换：新增「自动识别」输入格式——按格式前缀 / `(a,b,c)` 元组自动判定 HEX / RGB / HSL / HSV / CMYK / LAB / LCH / XYZ，取色器选色时自动识别默认按 HEX 处理
- 颜色格式转换：设置中心 → 类型转换 新增「颜色格式转换」面板，可配置进入页面时默认选中的类型（默认「自动识别」）
- `just dev-init`：一键检查 / 安装开发环境所需组件（Node.js ≥18、前端依赖、Rust 工具链、后端依赖），已就绪的组件打绿色 ✓ 并跳过，幂等可重复执行

### 🛠 体验优化

- 颜色格式转换：「自动识别」置于类型选项首位并设为默认选中；输入框下方实时显示已识别到的格式（如 `自动识别: LAB`）
- 开发文档：三个 README 的「开发」节补充 just 各平台安装命令，初始化流程整理为 `git clone` → `cd` → `just dev-init` → `just dev`
- README 项目结构展开 `src/App/` 子目录与逐文件说明，并补全 110 个工具的分类清单

# MagicTools v2.7.2

## 更新内容

### ✨ 新增

- Web 版（网页演示）支持 URL `?lang=` 参数指定界面语言（`?lang=en` / `?lang=zh-CN` / `?lang=zh-TW`，优先级高于本地记忆的语言设置），便于官网 / 外部链接直达指定语言界面
- 官网 demo 入口按钮按页面语言自动携带对应 `?lang=` 参数

### 🛠 体验优化

- 颜色格式转换：「格式 / 配色方案」页签移到输入框下方，输入区常驻顶部，切换页签不再影响输入
- 语言词条去公共化：删除 App 级共享 `ui-lang` / `crypto-lang` / `image-lang` / `webmaster-lang` 词条表，各工具词条拆回自己的 `lang.ts`（便于独立维护；界面文案与译文行为不变）

### 🐛 修复

- 颜色格式转换：取色器解析修正——antd `onChange` 第二参数是 CSS 字符串而非 hex，改用 `value.toHexString()`；修复选择颜色后输入框填充值非 `#rrggbb`、且不生成配色方案的问题
- 颜色格式转换：LAB / LCH / XYZ 等含负值通道的格式（如 lab 的 a/b 为负）解析失败，导致无转换结果与无配色方案；取色器改为直接用所选色 HEX 生成结果，避免文本回读的舍入漂移；顺带修复 HSV 误用 hsl 转换的旧问题

# MagicTools v2.7.1

## 更新内容

### ✨ 界面全面三语化（简中 / 繁中 / English）

- 全部工具页面接入语言切换：值计算 / 加解密 / 编解码 / 格式化 / 图片生成 / 站长工具 / 其它等各分类，新增公共 `ui-lang` / `rows-lang` 三语词条表（合计 470+ 条）
- 设置中心所有工具面板文案与「单位 / 下拉选项」三语化（如 次/字节/位 等单位、转换器单位表、码型、编辑器风格、默认外观、配色板、颜色名称等选项）
- 托盘菜单（原生）文案随界面语言动态切换
- 「About / 说明」区三语化：正则表达式（逐行测试说明）、Morse、JWT、BCD、BaseX（含各码型详述）、HTTP Basic Auth、Punycode / UUencode 等
- Cron 规则「解析」页签与解析引擎支持按语言输出；Hash 等设置项英文标签措辞调整（Show a default sample string → Sample String）
- 帮助页「开发时间线」全部历史版本内容三语化
- 侧边菜单分组英文名精简（Formatters / Value Calc 等）

### 🛠 体验优化

- 侧边栏展开宽度 200→230px；折叠时语言 / 设置按钮靠左下角，展开时紧贴靠右；语言菜单改用 cnode 旗标图片
- 设置-格式化分组移除「暂无独立设置项」提示文案

### 🐛 修复

- 修复多处残留硬编码中文与显示问题：Base64 图片输出类型「IMG 标签」、CSS 配色「颜色名称」等选项；RC2 / RC6 / Blowfish 与 Markdown 编辑器未显示在侧边菜单；字幕格式转换桌面版下载不再逐个弹保存框；深色模式下固定灰字提示不可见；左下角版本号多余悬停提示等

# MagicTools v2.7.0

## 更新内容

### ✨ 新增功能

- 二维码 / 条形码生成新增**「批量」tab**：多行内容一次生成并网格预览，桌面版选择文件夹一次导出全部 PNG（不再逐个弹保存框），网页版逐个下载
- 格式化分类新增**Markdown 编辑器**：自研渲染（零新增依赖），支持常用语法与行内 / 块级 LaTeX 公式子集，可导出 .md 与自带样式的独立 .html；预览面板可一键隐藏专注编辑
- 其它分类新增**密码生成**：安全随机生成（crypto，可排除易混淆字符）+ 启发式强度检测（熵估算 / 常见弱口令库含 leet 变体与键盘序列 / 在线与离线破解时间估算）
- 站长工具新增：**Sitemap 检测**（站点地图抓取与链接校验）、**关键词密度**（词频统计）
- 编码分类新增**路径转树**（目录结构文本 ↔ 树形展示）
- **Shield Badge 生成**新增图片尺寸（1–10 倍率）设置与放大 PNG 导出，预览同步倍率
- 菜单分类调整：占位图片 / Shield Badge / App Icon / ICO 生成迁入**图片**分类，CIDR 计算器迁入**其它**分类，设置页分组同步迁移（站长工具设置分组已清空删除）

### 🐛 修复

- 修复 RC2 / RC6 / Blowfish 与 Markdown 编辑器未出现在侧边菜单的问题（define 具名导出不一致 / 文件缺失）
- 修复字幕格式转换桌面版下载不弹保存框（下载锚点入 DOM + 延迟回收 Blob URL）
- 深色模式下密码强度检测、批量导出等固定灰字提示不可见（改用 antd theme token）
- 左下角版本号移除多余悬停提示（保留新版本角标）

### 🏗 工程

- capabilities 新增 `dialog:allow-open`，支持批量导出选文件夹写入
- 版本号升至 v2.7.0

# MagicTools v2.6.0

## 更新内容

### ✨ 新增功能

- **字幕格式转换**（新工具）：SRT / VTT / SBV / SUB / SSA / ASS / SMI / LRC / JSON 九种字幕格式互转，纯本地解析（内容不上传服务器），样式指令（ASS 特效、SRT <i>、MicroDVD {y:..} 等）自动剥除仅留文本与时间轴；自动检测输入格式，MicroDVD 可设帧率
- 新增**「格式化」分类**：JSON / JSON5 / SQL 格式化移入独立分类（侧边菜单、应用中心、设置页分组同步）
- 加解密新增：**ChaCha20**（RFC 7539，256 位密钥 + 96 位 nonce + 32 位计数器，OpenSSL/RFC 官方向量验证）与 **RC5 / RC2 / Blowfish / RC6**（统一参考 AES 的五模式/填充/编码 UI）
- 值计算新增：**CMAC**（SP 800-38B）/ **HKDF**（RFC 5869）/ **KMAC**（SP 800-185）与**原码/反码/补码计算**
- 颜色格式转换新增**配色方案**：相似/分离/三角/四角/方形/复合/双分离 7 组色相旋转展示（主色居中，点击复制 HEX）
- 编解码新增：**HTTP Basic Auth** 编解码 与 **Chmod 权限**
- 其它新增：**IP 转换**（IPv4 ↔ 整数/HEX/BIN 双向，支持 0x 输入，大端网络序）、**OTP 密码生成器**（TOTP/HOTP，SHA-1/256/512，Base32，otpauth URI 与扫码导入）、**键盘按键信息**、**点阵字生成器**、**文件比较**
- 配置转换支持 **XML 互转** + 配置文件拖入自动识别格式
- 时间工具新增 GPS / 北斗 / 伽利略（周/秒/总秒）/ 格洛纳斯 / 儒略日输出
- **新版本检测**：启动后自动查询 GitHub Releases，有新版时右下角弹窗提示（点击直达下载页；同版本仅提示一次，网络失败静默）

### 🐛 修复

- 应用中心分类内卡片一行展示多个（组内 flex 布局），分割线独占整行且名称显示正常
- 修复 tsc 全量门禁误扫构建产物导致 Debug Failure，与 import.meta/顶层 await 在 commonjs 下的误报（工程基建）

### 🏗 工程

- vendor-antd 随页面增多增至 ~950k，chunk 告警阈值提至 1000 并更新注释
- GitHub Actions 升级消除 node20 deprecation（checkout@v5 / setup-node@v5 / action-gh-release@v3 / upload-pages-artifact@v5 / deploy-pages@v5）
- Cloudflare Pages 构建：新增 `package` 脚本（vite build 别名）适配面板构建命令
- 版本号升至 v2.6.0

# MagicTools v2.5.0

## 更新内容

### ✨ 新增功能

- 值计算新增：**BCrypt** 口令生成与校验（cost 4–15，独立新页面）、**Scrypt**（RFC 7914，SHA-256/HMAC/PBKDF2/Salsa20-8/BlockMix/ROMix 零依赖纯 TS 实现，N/r/p/长度可调）、**PPI 计算**（标准 RGB 排列与 Pentile 等效 PPI，13 种常用手机/平板/笔记本/显示器屏幕预设）
- 加解密新增：**Cisco Type 7** 口令加解密（48 字节密钥表 XOR，2 位 hex 盐偏移）
- 编解码新增：**Gzip 编解码**（压缩率统计，压缩结果 Base64/Hex 双格式展示，解压自动识别 1F 8B 头）
- 站长工具新增：**占位图片生成**（含常用尺寸预设，设置页可自定义）与 **Shield Badge 生成**（shields.io 风格 SVG）；设置页新增站长工具分类 tab；App Icon 三平台可勾选、仅打包选中平台
- 其它分类新增：**ASCII 图片生成**（Rec.709 灰度 + 亮度/对比度/反色实时调节）与 **ASCII 文字**（figlet 风格 5x5 位图大字，可放大/调字距）
- Hash 值计算页移除 bcrypt 算法区（迁移为独立的 BCrypt 工具）
- 侧边菜单展开时分类标题显示所属应用数量徽标

### 🛠 体验优化

- BaseX 码型切换时下方说明联动显示对应编码详解（16 种码型）
- 设置按钮移至侧栏底部靠左（顶部仅留收起/应用中心）

### 🐛 修复

- 修复 App Icon 生成选中卡片深色模式下文字不可见
- 修复面包屑分类名称误带菜单应用数徽标
- 修复 `just release`：copy-to-release.js 项目根路径上溯层级错误（迁移 .erb 后 `..` 多一级）；默认构建不再把 bundle 残留的 msi/nsis 安装包复制进 release/（按目标筛选）

### 🏗 工程

- 删除 electron-react-boilerplate 遗留的 `.erb` 目录（必需文件迁移至项目根并同步 jest 配置）
- 版本号升至 v2.5.0

# MagicTools v2.4.1

> ⚠️ **本版发布原因（重要）**：上一版 **v2.4.0** 发布后，用户反馈**打开应用为黑屏、无任何内容**。经排查定位为渲染进程 webpack → Vite 迁移引入的产物兼容问题，本版已修复并作为正式版重新发布（v2.4.0 发布包已撤回）：
>
> 1. **残留 CommonJS `require()`**：源码中 3 处 `require('base-x' / 'toml-patch' / 'deepmerge')` 在 Vite/Rollup 下不再被转换、原样进入浏览器产物，执行即抛 `require is not defined`，导致 React 无法挂载（整页空白）→ 已全部改写为 **ESM import** 并补充类型声明；
> 2. **Tauri CSP 拦截 antd 运行时样式**：Tauri 会把配置的 CSP 收紧为 nonce 白名单（`style-src 'self' 'nonce-…'`），而 antd v5 样式是**运行时注入的 `<style>`**，会被全部拦截 → 已调整 CSP 不再注入 nonce 强化（页面保留 `script-src` 约束）。
>
> 修复效果经打包产物 + WebView2 远程调试实测：首页与 ConfigConvert / Base58Codec / BaseXCodec / SQLFormatter / Setting 等懒加载页面均可正常渲染（React 挂载、47 个样式表注入、零报错）。**请 v2.4.0 用户重新下载本版**。
>
> 另：上一版 v2.3.0 仅升级了版本号但未发布（缺 git tag / GitHub Release），其间的功能变更一并收录在本版发布中。

## 更新内容

### ✨ 新增功能

- 站长工具（**新分类**）：网页 TDK 信息检测 与 robots.txt 生成
- 编解码新增：**摩斯码编解码**（Web Audio 播放，红色高亮当前码值 + 电报音/蜂鸣/柔和/电子多音效 + 常用编码快速填充 + 一键保存 WAV 并弹窗选择保存位置）
- 编解码新增：**UUencode / XXencode** 编解码 与 **JWT 解码器**
- 加解密新增：**RSA 加解密** / **SM2 国密**（含密钥对生成与导出）、**SM4 加解密**、**AES GCM 认证加密模式**、古典密码 **Caesar / Rail Fence / Vigenere / Hill**
- 值计算新增：**SHA3 Hash**（SHA3-224/256/384/512 + SHAKE128/256）与 **Keccak Hash**（Keccak-224/256/384/512）
- 其它新增：**htpasswd 生成**（bcrypt / $apr1$ / {SHA} / 明文，可保存 .htpasswd）、**正则表达式工具**、**Cron 规则生成**（Linux 格式自动隐藏秒/年）

### 🛠 体验优化

- 正则表达式：常用正则预设增至 17 条（新增中文字符/双字节字符/网址/邮编/QQ 号码），预设列表与设置页可**一键复制规则**，按行红绿高亮匹配
- Cron 规则生成：新增「解析」页签，**逐字段中文解读** + 未来 10 次触发时间预览
- SHA3 / Keccak Hash 顶部示例与 Hash 值计算**共用设置**（值计算一致）

### 🐛 修复

- **修复 v2.4.0 安装包启动黑屏（本版发布原因）**：残留 CommonJS `require` 改写 ESM import、CSP 调整兼容 antd 运行时样式（详见顶部说明）
- 修复 App 列表加载竞态导致个别应用（如网页 TDK 检测）打不开的问题
- 修复网页 TDK 信息检测结果区域深色模式下内容不可见（改用主题 token 配色）

### ⚙️ 设置

- 设置页改为 **VSCode 式布局**：分类导航移至左侧栏，并记住上次浏览的分类

### 🏗 工程

- 渲染进程构建由 **webpack 迁移至 Vite**（工具页面懒加载分包、体积与行为保持一致，构建更快）

## 安装包说明

| 平台 | 文件 | 说明 |
| --- | --- | --- |
| Windows | `magic-tools.exe` | 单体免安装，双击即运行（依赖系统自带 WebView2） |
| macOS | `MagicTools-macOS-aarch64.zip` | 免安装，解压后拖入"应用程序"或直接运行 |
| Linux | `*.AppImage` | 免安装，`chmod +x` 后运行 |

---

# MagicTools v2.2.0

## 更新内容

### ✨ 新增功能

- 编解码分类新增 **Punycode 编解码**
- 值计算分类新增 **BCC 校验**（逐字节异或 XOR，支持 HEX/ASCII 双输入，HEX/DEC/OCT/BIN 四进制输出）
- 值计算分类新增 **LRC 校验**（累加和 / Modbus 二进制补码两种算法）
- 值计算分类新增 **CRC 校验**：44 种标准 CRC-3 ~ CRC-64 参数化计算（CRC-8/10/11/12/16/17/21/24/31/32/64 各标准变体，参考 ip33.com/crc.html），支持自定义算法参数展示与 check 自检

### 🛠 体验优化

- BCC/LRC/CRC 校验结果改为 Hash 风格输出行，**点击结果即复制**（移除冗余复制按钮）
- CRC 算法下拉加宽、选项内**多项式公式右对齐**、支持按名称/公式搜索，深色模式适配
- CRC 参数区两行布局：Width/Poly/Init/XorOut 输入框（点击复制）+ 输入数据反转(RefIn)/输出数据反转(RefOut) 勾选框

### ⚙️ 设置

- 设置「值计算」新增 BCC / LRC / CRC 分区：默认输入格式（**默认 ASCII/文本**）
- 设置新增 CRC **默认校验算法**（默认 CRC-16/MODBUS）

## 安装包说明

| 平台 | 文件 | 说明 |
| --- | --- | --- |
| Windows | `magic-tools.exe` | 单体免安装，双击即运行（依赖系统自带 WebView2） |
| macOS | `MagicTools-macOS-aarch64.zip` | 免安装，解压后拖入"应用程序"或直接运行 |
| Linux | `*.AppImage` | 免安装，`chmod +x` 后运行 |

---

# MagicTools v2.1.1

## 更新内容

### ✨ 重构与体验

- 代码目录重构：`src/renderer` 上移到 `src` 根目录（`App/hook/layout/lib` 直接位于 `src/` 下）
- Tab 切换保活：切换标签页不再销毁原页面，**已填写的数据不丢失**（关闭标签才卸载）
- 帮助页更新日志新增 V2.1.0 / V2.1.1 发布记录

### 📷 图片保存

- 条形码生成：下载 PNG 弹出**系统保存对话框**选择保存位置（WebView2 下 `<a download>` 不再可靠）
- 二维码生成：新增「保存图片」按钮，点击二维码预览同样可保存 PNG

## 安装包说明

| 平台 | 文件 | 说明 |
| --- | --- | --- |
| Windows | `magic-tools.exe` | 单体免安装，双击即运行（依赖系统自带 WebView2） |
| macOS | `MagicTools-macOS-aarch64.zip` | 免安装，解压后拖入"应用程序"或直接运行 |
| Linux | `*.AppImage` | 免安装，`chmod +x` 后运行 |

---

# MagicTools v2.0.0

## 更新内容

### ✨ 核心重构

- 从 Electron 迁移到 Tauri 2：应用体积更小（exe 约 11MB）、内存占用更低、启动更快
- 界面与功能保持不变（温度/距离/速度/面积/容积/质量转换、配置文件转换、GPS 转换等）

### 🚀 构建与发布

- 引入 `just` 命令统一管理开发/构建/发布流程（`just dev` / `just build` / `just release` / `just help`）
- 新增 GitHub Actions 自动打包：推送 `v*` 版本 tag 自动构建 Windows / macOS / Linux 三平台免安装包并发布到 GitHub Releases
- Windows 提供**单体免安装 exe**（双击即运行，依赖系统自带 WebView2）
- Linux 提供免安装 **AppImage**

### 🐛 修复

- 修复生产构建产物残留 source maps 的问题
- 移除页面四周白边，禁用右键菜单

## 安装包说明

| 平台 | 文件 | 说明 |
| --- | --- | --- |
| Windows | `magic-tools.exe` | 单体免安装，双击即运行（依赖系统自带 WebView2） |
| macOS | `MagicTools-macOS-aarch64.zip` | 免安装，解压后拖入"应用程序"或直接运行 |
| Linux | `*.AppImage` | 免安装，`chmod +x` 后运行 |
