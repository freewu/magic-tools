const intro = 
`
<pre>
## BCC 校验说明
\`\`\`
BCC (Block Check Character, 块校验字符) 是一种简单的差错校验方式: 把数据块中
每个字节做 按位异或 (XOR) 累加, 得到一个单字节 (0x00-0xFF) 的校验值
常用于串口通信 / Modbus / 扫码枪 / 工控协议等帧格式, 用于在收尾端快速发现
数据传输错误 (奇偶翻转/丢字节等)

计算方式 (起始值固定 0x00):
  BCC = 字节1 XOR 字节2 XOR 字节3 ... XOR 字节N
\`\`\`

## 示例
\`\`\`
数据帧:  01 03 00 00 00 02
计算:    0x01 ^ 0x03 ^ 0x00 ^ 0x00 ^ 0x00 ^ 0x02 = 0x00
BCC:     00      (拼在帧尾发送: 01 03 00 00 00 02 00)

数据帧:  AA 55
BCC:     FF
\`\`\`

## 使用说明
\`\`\`
1 输入方式: 支持两种
  - HEX 模式: 粘贴十六进制字节 (仅数据区, 不含起始符/帧尾/已有 BCC);
    支持空格、逗号、分号、冒号、竖线、短横线、下划线、换行分隔, 每段可带 0x 前缀,
    单字符按高位补 0 (如 F = 0x0F)
  - ASCII / 文本 模式: 直接输入文本 (如 ABC), 按 UTF-8 编码为字节参与异或,
    适合先看报文 ASCII 部分再拼帧的场景
2 BCC 值自动实时计算, 结果同时给出 HEX / DEC / OCT / BIN 四种进制, 双击任一值可复制
3 若协议有约定的期望值 (十六进制), 填到「期望 BCC」输入框自动比对
4 结果默认复制的是 HEX 形式 (协议帧尾最常用)

提示: 不同协议对 BCC 的初值/范围定义略有差异 (如固定异或 0xFF 后取反等),
本工具按最通用的"起始 0x00 直接逐字节异或"实现
\`\`\`
</pre>
`;
const BCCIntro = () => {

  return (
    <div 
      dangerouslySetInnerHTML={{ __html: intro }}
      style={ { "overflowY": "scroll","height": "260px" }}>
    </div>
  );
}

export default BCCIntro;